import pygame
import settings

gameColor = #FFFFFF or some color
userColor = #FFFFFF

def displayText(text):
    text = f'\n> {text}\n'
    return text

def customInput(text, type): #type = 'game' or 'user'
    return text